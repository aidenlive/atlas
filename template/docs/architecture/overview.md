---
title: Architecture overview
kind: explainer
owner: "{{OWNER}}"
status: draft
updated: "{{DATE}}"
review_by: "{{REVIEW_DATE}}"
audience: [internal]
summary: "The parts of this project and how they fit together."
---

# Architecture overview

One paragraph on what the parts are. Then a diagram, because any project with
more than one moving part owes its reader one (PRESENTATION `P-08`).

```text
[ inputs ] ──▶ [ the thing ] ──▶ [ outputs ]
```

Replace the sketch with a real diagram, drawn as code so its diff reviews like
prose.
