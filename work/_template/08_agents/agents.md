# Agents

## Assignments

| Agent | Tasks | Constraints | Expires |
|---|---|---|---|
| | | | |

An agent works under the authority ADMIN grants it, and no more (`W-18`).
Exactly one principal sequences several agents (`W-15`).

## Handoffs

A handoff is an artifact, not a conversation (`W-16`): what is done, what is in
flight, what is blocked and on whom.

### <date> — <from> → <to>
